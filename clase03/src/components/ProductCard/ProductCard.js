import React from 'react'

const ProductCard = ({ title, id, category, description, image, price }) => {
  return (
    <div className="card" style={{ width: '18rem' }}>
      {/* TODO: Crear la Card con forma de producto. */}
      ProductCard
    </div>
  )
}

export default ProductCard